---
interface Props extends astroHTML.JSX.HTMLAttributes {
  controlId: string;
}

const { id: _, controlId, ...attributes } = Astro.props;
---

<div
  id={`panel-${controlId}`}
  role="tabpanel"
  aria-labelledby={controlId}
  {...attributes}
>
  <slot />
</div>
